# URL base da API backend (Railway)
VITE_API_URL=http://localhost:3001/api

# Chave de API interna (mesma do backend — NÃO é um secret real exposto ao público,
# pois o sistema é de uso interno. Em produção, use variável de ambiente no Vercel.)
VITE_API_KEY=sua_api_key_interna_aqui
